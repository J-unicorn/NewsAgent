name: Daily News Crawler (Scrapling Final)

on:
  push:
    branches:
      - test_scrapling
    paths-ignore:
      - '**.md'
      - 'docs/**'
  workflow_dispatch:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
  PYTHONUNBUFFERED: 1
  PYTHONIOENCODING: utf-8

jobs:
  crawl:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    
    strategy:
      fail-fast: false
      max-parallel: 3
      matrix:
        site:
          - AITIMES
          - TECHWORLD
          - BUSINESSPOST
    
    steps:
      - name: Checkout test_scrapling
        uses: actions/checkout@v4
        with:
          ref: test_scrapling
      
      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
      
      # 🆕 최신 Scrapling 강제 설치
      - name: Install Scrapling (latest)
        run: |
          python -m pip install --upgrade pip
          # 최신 버전 설치 (>=0.4)
          pip install --upgrade "scrapling[fetchers]"
          pip install pandas
          
          # 버전 확인
          python -c "import scrapling; print(f'Scrapling version: {scrapling.__version__}')"
      
      # 🆕 BUSINESSPOST는 StealthyFetcher 필요 → Playwright 브라우저 설치
      - name: Install Scrapling browsers (StealthyFetcher)
        if: matrix.site == 'BUSINESSPOST'
        run: |
          # Scrapling 공식: 브라우저 자동 설치
          scrapling install
          
          # 백업: 직접 설치
          # python -m playwright install chromium --with-deps
      
      - name: Show environment
        run: |
          python --version
          pip list | grep -iE "scrapling|playwright|lxml|cssselect"
      
      - name: Create output directory
        run: mkdir -p output
      
      - name: Run crawler for ${{ matrix.site }}
        run: |
          echo "=== ${{ matrix.site }} 크롤링 시작 ==="
          time python -u news_collector.py 2>&1 | tee output/${{ matrix.site }}_run.log
        env:
          SITE: ${{ matrix.site }}
        continue-on-error: true
      
      - name: Show last logs
        if: always()
        run: |
          ls -lah output/ || echo "No output"
          if [ -f output/${{ matrix.site }}_run.log ]; then
            echo ""
            echo "=== Last 30 lines ==="
            tail -30 output/${{ matrix.site }}_run.log
          fi
      
      - name: Upload artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: scrapling-${{ matrix.site }}-${{ github.run_id }}
          path: output/
          retention-days: 7
          if-no-files-found: warn

  merge:
    needs: crawl
    runs-on: ubuntu-latest
    if: always()
    timeout-minutes: 5
    
    steps:
      - uses: actions/checkout@v4
        with:
          ref: test_scrapling
      
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - run: pip install pandas
      
      - uses: actions/download-artifact@v4
        with:
          path: ./all-results
          pattern: scrapling-*-${{ github.run_id }}
      
      - name: Merge CSVs
        run: |
          mkdir -p merged
          python << 'PYEOF'
          import pandas as pd
          import glob
          import os
          import re
          from datetime import datetime
          
          all_csvs = glob.glob('all-results/**/*.csv', recursive=True)
          all_csvs = [c for c in all_csvs if not c.endswith('_empty.csv')]
          print(f"Found {len(all_csvs)} CSV files")
          
          if not all_csvs:
              print("⚠️ No CSV files")
              exit(0)
          
          dfs = []
          for csv in sorted(all_csvs):
              try:
                  df = pd.read_csv(csv, encoding='utf-8-sig')
                  if not df.empty:
                      dfs.append(df)
                      print(f"  ✓ {csv}: {len(df)} rows")
              except Exception as e:
                  print(f"  ✗ {csv}: {e}")
          
          if not dfs:
              exit(0)
          
          merged = pd.concat(dfs, ignore_index=True)
          
          if 'title' in merged.columns:
              def normalize_title(t):
                  if pd.isna(t): return ""
                  return re.sub(r'\s+', '', str(t)).lower()
              
              merged['_norm'] = merged['title'].apply(normalize_title)
              before = len(merged)
              merged = merged.drop_duplicates(subset=['_norm'], keep='first')
              merged = merged.drop(columns=['_norm'])
              print(f"🧹 중복: {before} → {len(merged)}")
          
          if 'date' in merged.columns:
              merged = merged.sort_values(by=['date'], ascending=False, na_position='last')
          
          today = datetime.now().strftime('%y%m%d_%H%M%S')
          output = f'merged/{today}_competitor.csv'
          merged.to_csv(output, index=False, encoding='utf-8-sig')
          print(f"\n✅ {output}: {len(merged)} rows")
          
          if 'provider' in merged.columns:
              print(merged['provider'].value_counts())
          PYEOF
      
      - uses: actions/upload-artifact@v4
        with:
          name: scrapling-merged-final-${{ github.run_id }}
          path: merged/*.csv
          retention-days: 30
          if-no-files-found: warn
